nav.navbar.navbar-expand-md.navbar-light.fixed-top.bg-light.d-flex.justify-content-between
        div
            a.navbar-brand(href='/') Simply
            button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbarCollapse', aria-controls='navbarCollapse', aria-expanded='false', aria-label='Toggle navigation')
                span.navbar-toggler-icon
        div
            #navbarCollapse.collapse.navbar-collapse.text-right
                ul.navbar-nav.mr-auto
                    li.nav-item
                        a#nav-register.nav-link(href="#" data-section="signup") Register
                    li.nav-item
                        a#nav-signin.nav-link(href="#" data-section="login") Sign in
                    li.nav-item
                        a#nav-viewData.nav-link(href='/events') View Data
                    li.nav-item
                        a#nav-createEvent.nav-link(href="#" data-section="createEvent") Create Event
                    li.nav-item
                        a#nav-joinEvent.nav-link(href="#" data-section="joinEvent") Join Event
                    li.nav-item
                        a.nav-link(href='/user') Dashboard